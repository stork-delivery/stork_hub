/// Environment configuration for the app
class AppEnvironment {
  /// Creates a new [AppEnvironment] instance
  const AppEnvironment({
    this.storkUrl,
  });

  /// The URL for the Stork service
  final String? storkUrl;
}
